# Backend Environment Variables
# Update with your actual values

# Server Configuration
PORT=5001

# Email Configuration (Gmail SMTP)
# Get these from your Gmail account settings
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Instructions for Gmail App Password:
# 1. Go to your Google Account settings
# 2. Enable 2-Factor Authentication if not already enabled
# 3. Go to Security > App passwords
# 4. Generate a new app password for "Mail"
# 5. Use this app password in the EMAIL_PASS field above
# 6. Never use your regular Gmail password for this

# Firebase Configuration
# The Firebase admin configuration is handled by firebase-admin.json file
# Make sure firebase-admin.json is properly configured in the config/ directory
